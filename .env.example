# Copy to `.env` and adjust for your environment.

# API
PORT=4000

# Postgres (used by both the `db` container and the api connection pool)
PGHOST=db
PGPORT=5432
PGUSER=todos
PGPASSWORD=todos
PGDATABASE=todos
